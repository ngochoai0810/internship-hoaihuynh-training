Day 26/6:

BASIC THEORY
    - Python is an interpreted language 
    - It mean the code is translated and execute line by line at runtime
    -It doesn't produce an executed (.exe)
    *Reason in py a statement not end by ; or , cause they are excute one by one

    2. Py indentation
    3. Py variables:
        - String, integer, float, bool, bytes, complex
        - Rule of variables:
            + must be start wth lettre or underscore char(_a)
            + cant start by num
            + only contain 0-9, A_z, _
            + case-sentitive ( age != Age != AGE)
            + cant be the Py keywords
        - Output Variables:
            + (str+str), (int+int), (int,str) but (int+str) is not egal

        - Global var: highest priority level
    4. Type Convert
        - Cant convert complex to other types
    5. Slice String
        - str[2:5] -> start at 2 and end at 4
    6. Modify Strings
        - a.upper() : hel -> HEL
        - .lower(): HELL -> hell
        - .strip(): remove whitespace
        - .replace(): H -> J
        - .split(): substring if it find ","
    7.Concatenation:
        - use {} to in: f-string
    8.Py Booleans
        - isinstance() : check an object is an integer or not
    9.Operator:
        - Modulus: %, Exponentiation: x**y
        - Division "/" return float, Floor division "//" return integer
        - comparison: 
        - chaining comparison: allow to chain comparison operator
        - logical operator: and not or








